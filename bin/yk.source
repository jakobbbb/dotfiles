if [ "$WY_OPT" = "pin" ]; then
    pass -c webis/yubi/pin
else
    eval $(ssh-agent)
    gpg-connect-agent updatestartuptty /bye
    cd ~/git/code-webis-cmd
    eval $(webis.py yk ssh connect-agent)
fi
